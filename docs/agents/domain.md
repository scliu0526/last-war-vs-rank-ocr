# Domain Docs

本專案採單一 context。

## Before exploring

- 讀取根目錄 `CONTEXT.md`。
- 讀取與工作範圍相關的 `docs/adr/`。
- 若文件不存在則繼續工作，不預先建立空白文件。

## Vocabulary

Issue、規格、測試與程式碼中的領域概念應使用 `CONTEXT.md` 定義的詞彙。若缺少必要詞彙，先確認它是真正的領域缺口，再使用 domain-modeling 流程補充。

## ADR conflicts

若工作內容與既有 ADR 衝突，必須明確指出，不得靜默覆寫既有決策。
