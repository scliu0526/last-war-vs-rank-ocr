# 採用自包含的離線 Windows 桌面程式

程式以 Windows x64 自包含套件交付，OCR 模型隨程式安裝，日常辨識不需網路，也不要求使用者另外安裝 Python、Office 或 OCR 工具。程式不提供帳號、雲端同步、遙測、使用分析、廣告、背景模型更新或自動錯誤上傳；圖片、OCR 結果與設定只保存在本機。此選擇接受較大的發佈體積，以換取排名截圖留在本機、部署依賴少且使用環境一致。
