# 採用 PP-OCRv5 與 ONNX Runtime

多語排名文字採用 PP-OCRv5 模型，透過 ONNX Runtime 的 C# 介面在本機執行，並允許對同一文字區域產生多語模型候選。此方案不依賴付費雲端服務，接受較大的發佈體積與較長辨識時間，以繁中、英文、韓文、泰文、日文混合內容的準確度優先。執行模式預設為 CPU；使用者可切換到 DirectML GPU 並指定顯示卡，不相容的 GPU 不得暗中降級為其他模式。
