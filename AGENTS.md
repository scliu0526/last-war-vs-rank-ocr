# AGENTS.md

用來減少 LLM 常見程式設計錯誤的行為準則。可視需要與專案特定指示合併使用。

**取捨：** 這些準則偏重謹慎而非速度。對於簡單的任務，請自行斟酌。

## 1. 編寫程式碼前先思考

**不要自行假設。不要隱瞞疑惑。明確說明各項取捨。**

開始實作前：

- 明確說明你的假設。如果不確定，請先詢問。
- 如果存在多種解讀方式，請全部提出，不要默默選擇其中一種。
- 如果有更簡單的作法，請提出來。必要時應表達不同意見。
- 如果有任何不清楚的地方，請先停止。指出讓你感到困惑之處，並提出問題。

## 2. 簡單優先

**使用能解決問題的最少程式碼。不加入任何推測性的內容。**

- 不要加入需求以外的功能。
- 不要為只使用一次的程式碼建立抽象層。
- 不要加入未被要求的彈性或可設定性。
- 不要為不可能發生的情境加入錯誤處理。
- 如果你寫了 200 行，但其實 50 行就能完成，請重新改寫。

問問自己：「資深工程師是否會認為這過於複雜？」如果答案是肯定的，請將它簡化。

## 3. 精準修改

**只修改必要的部分。只清理由你的修改所產生的問題。**

編輯現有程式碼時：

- 不要改善相鄰的程式碼、註解或格式。
- 不要重構沒有問題的部分。
- 即使你會採用不同的寫法，也應配合現有風格。
- 如果發現不相關的無用程式碼，請提出來，但不要將其刪除。

當你的修改產生不再使用的項目時：

- 移除因你的修改而不再使用的導入、變數或函式。
- 除非使用者要求，否則不要移除原本就存在的無用程式碼。

檢驗標準：每一行變更都應能直接追溯到使用者的需求。

## 4. 以目標驅動執行

**定義成功標準。持續執行直到完成驗證。**

將任務轉換為可驗證的目標：

- 「新增驗證」→ 為無效輸入撰寫測試，然後讓測試通過。
- 「修正程式錯誤」→ 撰寫能重現問題的測試，然後讓測試通過。
- 「重構 X」→ 確保重構前後的測試都能通過。

對於多步驟任務，先提出簡短計畫，並為每一步指定驗證項目。

## Agent skills

### Issue tracker

規格與任務使用 `scliu0526/last-war-vs-rank-ocr` 的 GitHub Issues。詳見 `docs/agents/issue-tracker.md`。

### Triage labels

使用專案定義的五種 triage labels。詳見 `docs/agents/triage-labels.md`。

### Domain docs

本專案採單一 context；工作前讀取根目錄 `CONTEXT.md` 與相關 `docs/adr/`。詳見 `docs/agents/domain.md`。
